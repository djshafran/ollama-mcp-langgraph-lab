services:
  l0:
    build:
      context: .
      dockerfile: src/l0/server/Dockerfile
    environment:
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
      - L0_BACKEND=${L0_BACKEND:-heritage}
      - L0_ARTIFACT_DIR=/artifacts/current
      - L0_DATA_DIR=/data
      # Heritage local mode (optional overrides)
      # - HERITAGE_METHOD=shell
      # - HERITAGE_BASE_DIR=/opt/Heritage_Platform
      # - HERITAGE_LEXICON=...
    ports:
      - "8000"
    volumes:
      - ./src/l0/artifacts:/artifacts
      - ./src/l0/data:/data:ro
      - ./third_party/heritage/Heritage_Platform:/opt/Heritage_Platform:ro
